//program to find area of rectangale (input is not from user)
#include<stdio.h>
int main()
{
    int length=30,breadth=4;
    int area=length*breadth;
    printf("the area of rectangle %d",area);
    return 0;
}